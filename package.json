{
  "name": "chat-app",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^your-version",
    "socket.io": "^your-version",
    "express-session": "^your-version",
    "connect-sqlite3": "^your-version",
    "sqlite3": "^your-version"
  }
}
